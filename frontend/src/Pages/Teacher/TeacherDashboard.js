import React from 'react';
import PageLayout from '../../Components/PageLayout';

function TeacherDashboard({ role }) {
  return <PageLayout title="Teacher Dashboard" role={role} />;
}

export default TeacherDashboard;
