import React from 'react';
import PageLayout from '../../Components/PageLayout';

function StudentDashboard({ role }) {
  return <PageLayout title="Student Dashboard" role={role} />;
}

export default StudentDashboard;
